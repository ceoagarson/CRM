{
  "name": "crm",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm install && cd server && tsc && cd ../client && npm run build && mv build ../server/dist/build ",
    "start": "node server/dist/app.js"
  }
}